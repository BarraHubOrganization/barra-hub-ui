import React from "react";
interface Props {
    /** Texto a ser exibido dentro do botão. */
    loading: boolean;
}
export default function Loader({ loading }: Props): React.JSX.Element;
export {};
